package com.gmwapp.hima.callbacks

/**
 * Implemented by fragments that can be refreshed when the user
 * re-taps the bottom navigation tab they're already on.
 */
interface Refreshable {
    fun refresh()
}
