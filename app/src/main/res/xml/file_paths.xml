<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Camera capture for custom profile avatar (EditProfileActivity) -->
    <files-path name="avatars" path="avatars/" />
    <!-- Tester log share (LogCapture writes to external app-specific storage) -->
    <external-files-path name="logs" path="logs/" />
</paths>
