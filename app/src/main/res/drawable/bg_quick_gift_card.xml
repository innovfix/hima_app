<?xml version="1.0" encoding="utf-8"?>
<!-- Inline in-call quick-gift card — dark translucent fill + subtle border,
     matching the connected-call "Tap to send" row. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#24203A" />
    <corners android:radius="16dp" />
    <stroke
        android:width="1dp"
        android:color="#393354" />
</shape>
