<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="end"
    android:paddingVertical="3dp"
    android:clipToPadding="false"
    android:clipChildren="false">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:clipChildren="false">

        <LinearLayout
            android:id="@+id/bubble_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="14dp"
            android:minWidth="56dp"
            android:orientation="vertical"
            android:maxWidth="280dp"
            android:background="@drawable/chat_bubble_sender"
            android:paddingHorizontal="12dp"
            android:paddingTop="8dp"
            android:paddingBottom="6dp"
            android:elevation="1dp">

            <include
                android:id="@+id/layout_reply_quote"
                layout="@layout/view_reply_quote"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="6dp"
                android:visibility="gone" />

            <TextView
                android:id="@+id/tv_message"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="10"
                android:text="I'm doing great! Thanks for asking."
                android:textSize="14.5sp"
                android:textColor="@color/chat_text_sent"
                android:fontFamily="@font/poppins_regular"
                android:lineHeight="20dp"
                android:lineSpacingExtra="1dp" />

            <!-- CHAT-127: WhatsApp-style "Read more / Show less" toggle for long
                 messages. Shown only when [tv_message] is truncated by the
                 maxLines clamp above (or when the user has manually expanded). -->
            <TextView
                android:id="@+id/tv_read_more"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:background="?attr/selectableItemBackground"
                android:fontFamily="@font/poppins_semibold"
                android:paddingVertical="2dp"
                android:text="@string/chat_read_more"
                android:textColor="@color/chat_text_sent"
                android:textSize="12sp"
                android:visibility="gone" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="3dp">

                <TextView
                    android:id="@+id/tv_time"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="10:31 AM"
                    android:textSize="11sp"
                    android:textColor="@color/chat_time_sent"
                    android:letterSpacing="0.02"
                    android:fontFamily="@font/poppins_regular" />

                <ProgressBar
                    android:id="@+id/pb_send_pending"
                    style="?android:attr/progressBarStyleSmall"
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_marginStart="6dp"
                    android:indeterminateTint="@color/chat_time_sent"
                    android:visibility="gone" />

                <ImageView
                    android:id="@+id/iv_send_delivery"
                    android:layout_width="wrap_content"
                    android:layout_height="14dp"
                    android:layout_marginStart="4dp"
                    android:adjustViewBounds="true"
                    android:contentDescription="@null"
                    android:visibility="gone" />
            </LinearLayout>
        </LinearLayout>

        <TextView
            android:id="@+id/tv_reaction"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/bubble_container"
            android:layout_alignStart="@id/bubble_container"
            android:layout_marginTop="-10dp"
            android:layout_marginStart="-8dp"
            android:minWidth="28dp"
            android:minHeight="28dp"
            android:padding="6dp"
            android:gravity="center"
            android:text="👍"
            android:textSize="16sp"
            android:background="@drawable/bg_reaction_circle"
            android:elevation="3dp"
            android:visibility="gone" />

    </RelativeLayout>

</LinearLayout>


