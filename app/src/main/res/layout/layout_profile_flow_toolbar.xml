<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/profile_flow_toolbar_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="56dp"
    android:background="@color/white"
    android:elevation="2dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingTop="8dp"
    android:paddingEnd="16dp"
    android:paddingBottom="8dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/cv_back"
        android:layout_width="42dp"
        android:layout_height="42dp"
        app:cardBackgroundColor="#FFFFFF"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp">

        <ImageView
            android:id="@+id/iv_back"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:contentDescription="@string/edit"
            android:src="@drawable/ic_back"
            app:tint="@color/pink" />
    </androidx.cardview.widget.CardView>

    <!-- BUG 24 — maxLines=2, not 1. "Community Guidelines & Moderation Policy" (39 chars)
         needs ~366dp at 18sp but the title box is ~274dp on a 360dp screen, so a one-line
         cap could only ellipsize it. The root is already wrap_content + minHeight 56dp +
         gravity=center_vertical, so it grows on its own and the back card stays centred.
         Every other title here is 21 chars or fewer and still renders on one line;
         ellipsize stays as the backstop if a longer one ever lands. -->
    <TextView
        android:id="@+id/tv_flow_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:fontFamily="@font/poppins_semibold"
        android:maxLines="2"
        android:textColor="@color/black_light"
        android:textSize="18sp" />
</LinearLayout>
