<?xml version="1.0" encoding="utf-8"?>
<!-- One question row: numbered pink chip + question text -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:background="@drawable/icebreaker_qrow_bg"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="13dp"
    android:paddingTop="12dp"
    android:paddingEnd="13dp"
    android:paddingBottom="12dp">

    <TextView
        android:id="@+id/tv_q_number"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_marginEnd="11dp"
        android:background="@drawable/icebreaker_qnum_bg"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:textSize="11sp"
        android:textStyle="bold"
        tools:ignore="HardcodedText"
        tools:text="1" />

    <TextView
        android:id="@+id/tv_q_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:lineSpacingExtra="1dp"
        android:textColor="#3A3550"
        android:textSize="14sp" />
</LinearLayout>
