<?xml version="1.0" encoding="utf-8"?>
<!-- Themed toolbar matching Home / Profile aesthetic — back button left unchanged. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/profile_flow_toolbar_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:elevation="2dp">

    <!-- Decorative pink elements on top-right, matching Profile / Recent hero. -->
    <ImageView
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="14dp"
        android:layout_marginEnd="14dp"
        android:alpha="0.55"
        android:src="@drawable/ic_sparkle"
        app:tint="#E91E63" />

    <ImageView
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="30dp"
        android:layout_marginEnd="34dp"
        android:alpha="0.4"
        android:src="@drawable/ic_sparkle"
        app:tint="#E91E63" />

    <ImageView
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="44dp"
        android:layout_marginEnd="14dp"
        android:alpha="0.5"
        android:src="@drawable/ic_sparkle"
        app:tint="#E91E63" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="20dp"
        android:paddingEnd="16dp"
        android:paddingBottom="20dp">

        <!-- Back button — UNCHANGED. Same grey card + arrow as before. -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cv_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:cardBackgroundColor="#F1F5F9"
            app:cardCornerRadius="12dp"
            app:cardElevation="0dp">

            <ImageView
                android:id="@+id/iv_back"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:contentDescription="@string/edit"
                android:rotation="180"
                android:src="@drawable/right_arrow"
                app:tint="#0F172A" />
        </androidx.cardview.widget.CardView>

        <!-- Themed title — poppins_medium 22sp, matches "My Profile" / "Recent Calls" / "Hi ma". -->
        <TextView
            android:id="@+id/tv_flow_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:fontFamily="@font/poppins_medium"
            android:includeFontPadding="false"
            android:letterSpacing="-0.02"
            android:maxLines="1"
            android:textColor="#000000"
            android:textSize="22sp" />
    </LinearLayout>
</FrameLayout>
