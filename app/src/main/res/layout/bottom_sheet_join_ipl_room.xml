<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="#0F172A">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Join IPL Room"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:fontFamily="@font/poppins_bold"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Choose your team header -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Choose your team"
        android:textColor="#B0FFFFFF"
        android:textSize="13sp"
        android:fontFamily="@font/poppins_semibold"
        android:layout_marginBottom="10dp" />

    <!-- Team chips row (horizontal scroll, centered) -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:fillViewport="true"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:layout_marginBottom="18dp">

        <LinearLayout
            android:id="@+id/ll_team_chips"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="horizontal" />
    </HorizontalScrollView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_join_random"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Join Randomly"
        android:textSize="16sp"
        android:fontFamily="@font/poppins_semibold"
        android:textColor="@color/white"
        android:enabled="false"
        android:alpha="0.5"
        app:backgroundTint="#4CAF50"
        app:cornerRadius="14dp"
        app:icon="@drawable/baseline_arrow_forward_24"
        app:iconGravity="end"
        app:iconTint="@color/white"
        android:layout_marginBottom="12dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="OR"
        android:textColor="#80FFFFFF"
        android:textSize="14sp"
        android:gravity="center"
        android:fontFamily="@font/poppins_regular"
        android:layout_marginBottom="12dp" />

    <EditText
        android:textCursorDrawable="@drawable/color_cursor"
        android:id="@+id/et_invite_code"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:hint="Enter Invite Code"
        android:textColor="@color/white"
        android:textColorHint="#80FFFFFF"
        android:textSize="16sp"
        android:fontFamily="@font/poppins_semibold"
        android:inputType="textCapCharacters"
        android:maxLength="6"
        android:gravity="center"
        android:background="#1A2640"
        android:padding="12dp"
        android:layout_marginBottom="12dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_join_with_code"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Join with Code"
        android:textSize="16sp"
        android:fontFamily="@font/poppins_semibold"
        android:textColor="@color/white"
        android:enabled="false"
        android:alpha="0.5"
        app:backgroundTint="#2196F3"
        app:cornerRadius="14dp"
        app:icon="@drawable/baseline_arrow_forward_24"
        app:iconGravity="end"
        app:iconTint="@color/white" />

</LinearLayout>
