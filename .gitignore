*.iml
.gradle
/local.properties
/.idea/*
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
# Production release files
app/production/release/*.aab
app/production/release/*.apk
# Kotlin compiler caches
.kotlin/
# Android logcat captures dropped at repo root
*.logcat
# Claude / Cursor local agent state
.claude/
# QA artifacts dropped at repo root (bug reports, test-case docs, proof folders)
/HIMA_*
/*_proofs/
/autopay_api_test/
/build_bug_pdf.py
/RANJINI_DB_ACCESS_HANDOVER.md

# Local API testing artifacts (admin cookies, login dumps, captured response
# bodies) — sensitive, must never be pushed.
/autopay_api_test/

# 2026-05-22 — local signing config (contains keystore password, never commit)
app/keystore.properties
keystore.properties
